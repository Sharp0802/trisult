[package]
name = "trisult"
version = "0.1.0"
edition = "2024"

[features]
alloc = ["dep:smallvec"]

[dependencies]
smallvec = { version = "2.0.0-alpha.12", optional = true }
trisult-derive = { path = "../derive", version = "0.1" }
